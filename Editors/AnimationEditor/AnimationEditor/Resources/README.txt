if loading a new animation xml
	go into xml, 
		set <Object ObjectName="C/BLahblah/blah"> to <Object ObjectName=""> 
Open animation editor load xml via "Open" then load bitmap via "OpenBitmap"
Save will fill out bitmap path so loading in the future will only need the new xml file
add new xml to gitignore if you want